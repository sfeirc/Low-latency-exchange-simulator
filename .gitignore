# Build directories
build/
build-*/
cmake-build-*/
out/

# CMake generated
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
compile_commands.json
_deps/
install_manifest.txt

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store

# Large regeneratable inputs (synthetic/replayed market data) — regenerate
# via apps/synth_gen or apps/replay_tool, don't version.
data/recordings/*.bin
data/recordings/*.jsonl

# artifacts/*.json (benchmark results) and artifacts/*.html (the book
# animation) are intentionally NOT ignored: they're the receipts behind
# docs/benchmarks.md and a required deliverable, not build output.

# Unrelated local plugin state
ruvector.db

# ccache
.ccache/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
